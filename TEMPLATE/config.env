# ──────────────────────────────────────────────────────────────
#  HomeStack Config — config.env
#  Public defaults committed to git. Never put secrets here.
#  Pin images to a specific tag — never use :latest
# ──────────────────────────────────────────────────────────────

# Docker image (pin to a specific version!)
MYAPP_IMAGE=ghcr.io/org/myapp:1.0.0

# Host port for the web UI
MYAPP_PORT=8080
